<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginActivity"
    android:background="@color/profileDefault">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/login_profile"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_above="@id/layout_login"
        android:layout_centerInParent="true"
        android:clickable="true"
        android:padding="8dp"
        android:layout_marginBottom="24dp"
        android:src="@drawable/ic_baseline_image_24"
        app:civ_border_color="@color/white"
        app:civ_border_width="1dp" />

    <RelativeLayout
        android:id="@+id/layout_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center">

        <EditText
            android:id="@+id/login_nickname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginTop="24dp"
            android:ems="10"
            android:gravity="center"
            android:hint="닉네임 입력"
            android:inputType="text"
            android:maxLength="10"
            android:textColorHint="@color/white"
            android:textStyle="bold"
            android:layout_marginRight="8dp"/>

        <Button
            android:id="@+id/login_btn"
            android:layout_width="60dp"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/login_nickname"
            android:layout_marginBottom="12dp"
            android:layout_toRightOf="@id/login_nickname"
            android:backgroundTint="@color/drawerColor"
            android:text="확인"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="bold" />

    </RelativeLayout>



</RelativeLayout>